[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip3 install 'rembg[cpu]' pillow --quiet --break-system-packages"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start -p ${PORT:-3000}"
