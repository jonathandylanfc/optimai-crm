[phases.setup]
aptPkgs = ["python3", "python3-pip", "libgl1-mesa-glx", "libglib2.0-0"]

[phases.install]
cmds = ["pip3 install 'rembg[cpu]' pillow --quiet --break-system-packages", "npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
